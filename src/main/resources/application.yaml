server:
  port: 8080
spring:
  application:
    name: oa
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 123456
    username: root
    url: jdbc:mysql://localhost:3306/oa?&useSSL=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
mybatis-plus:
  configuration:
    cache-enabled: true
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
